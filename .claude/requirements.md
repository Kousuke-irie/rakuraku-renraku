# 要件一覧（要件ID・受入条件）

**P1 が終わるまで P2 以降に着手しない。** 時間不足時は P2・P3 を切り捨てる。

---

## P0｜基盤要件（Day1）

### A-1. メッセージの永続化
すべてのメッセージを SQLite に保存し、入室時に過去ログを取得する。詳細は `database.md`。

**受入条件**
- サーバを再起動してもメッセージが消えない
- ルームを開いた直後に過去のメッセージがすべて表示される
- 別ブラウザで同じアカウントにログインしても同じ履歴が見える

### A-2. 独立したバックエンド
Vite dev サーバ依存を解消し、Express + Socket.IO の独立プロセスへ分離する。
開発時は Vite の `server.proxy` で `/api` と `/socket.io` を `localhost:3000` へ転送。

**受入条件**
- `npm run build` した静的ファイルを Express から配信してもチャットが動作する
- Vite を停止しても API サーバ単体で REST が応答する

### A-3. 認証・セッション
ID＋パスワード認証（bcrypt cost 10）、JWT を httpOnly Cookie で発行。
**ルーターガードを認証状態ベースに置換する**（`frontend.md` 参照）。

**受入条件**
- ログイン後に F5 でリロードしてもログイン画面に戻されない
- 未ログインで `/inbox` に直接アクセスするとログイン画面へリダイレクトされる
- DB にパスワードが平文で保存されていない

### A-4. ルーム概念
全体ブロードキャストからルーム単位の配信（`socket.join`）へ変更する。
接続時に自分の所属する全ルームへ一括 join する。

**受入条件**
- 学生Aへのメッセージが学生Bの画面に表示されない
- 受信箱を開いたままでも、どのルームの新着でも一覧が更新される

### A-5. 状態管理（Pinia）
`provide/inject` のバケツリレーを廃止し、Pinia ストアへ集約する。詳細は `frontend.md`。

**受入条件**
- 任意のコンポーネントから props を経由せず未読数を参照できる
- ルーム切替時に既読済みメッセージが再フェッチされない

### B-1. トークリスト画面
→ **P1-1（受信箱）に統合。** 本プロダクトではトークリストが受信箱そのもの。別画面として実装しない。

### B-2. トーク画面 UI
吹き出し・アイコン・時刻・日付セパレータ・古い→新しい順・無限スクロール。詳細は `frontend.md`。

**受入条件**
- 自分と相手の発言が左右で明確に区別できる
- 100件以上の履歴があるルームで、上スクロールにより過去ログが継ぎ足し表示される
- 日付をまたぐ会話にセパレータが表示される

### B-3. メッセージ機能
既読管理・送信状態（送信中→送信済→既読）・失敗時の再送・24h以内の送信取消。

**受入条件**
- オフライン状態で送信すると失敗表示と再送ボタンが出る
- 再送してもメッセージが二重に保存されない
- 相手がルームを開くと自分の吹き出しに「既読」が1秒以内に付く

### B-5. プロフィール
表示名・ステータスメッセージの編集のみ。アイコンは `avatar_color` の選択。

**スコープ外**：ユーザーID検索、友だち追加・削除・ブロック、グループ作成・招待・退会

---

## P1｜Must（Day2 前半）★これが無ければコンセプトが成立しない

| ID | 要件 | 工数 | 実装順 |
| --- | --- | --- | --- |
| P1-1 | 学生一覧（受信箱）画面 | M | 1 |
| P1-2 | 対応ステータス | M | 2 |
| P1-3 | 選考ステータス | S | 3 |
| P1-4 | 経過時間バッジ | S | 4 |
| P1-6 | 緊急度の自動判定 | M | 5 |
| P1-7 | フィルタ＆ソート | M | 6 |
| P1-5 | 用件タグ（自動判定） | M | 7 |
| P1-8 | 未対応サマリーバー | S | 8 |

### P1-1. 学生一覧（受信箱）画面 ★主画面
ログイン後の初期表示。1行＝1学生。表示項目とレイアウトは `frontend.md` を参照。

**受入条件**
- ログイン直後にこの画面が表示される
- 別ブラウザから学生がメッセージを送ると、リロードせず該当行が最上位付近に移動し未読バッジが増える

### P1-2. 対応ステータス
`要返信 / 対応中 / 返信待ち / 完了 / 保留`。**一覧から1クリックで変更可能**、色で区別。
手動変更時は `type='system'` のシステムメッセージを記録する。

**受入条件**
- 一覧を離れずに2クリック以内でステータスを変更できる
- 他の人事の画面にも即座に反映される

### P1-3. 選考ステータス（ラベル）
`エントリー / 書類 / 適性検査 / 一次〜五次面接 / 内定 / 辞退`。**面接は5回**（実データ準拠）。

**受入条件**：一覧行に現在の選考段階が常に表示されている

### P1-4. 経過時間バッジ（SLAアラート）
学生の最終メッセージからの経過時間。**24h超で赤、12h超で黄**。判定は `business-logic.md`。
課題 C-1「合否連絡が1日遅れた」への直接の回答。

**受入条件**
- 24時間以上返信していない学生の行が赤で強調される
- 返信すると（`waiting_student` へ遷移し）バッジが消える

### P1-5. 用件タグ（自動判定）
`欠席・遅刻 / 日程調整 / 質問 / 適性検査 / 合否待ち / その他` をサーバ側で自動付与。
**学生にはタグを選択させない**（入力負担と誤選択の責任所在を回避）。辞書は `business-logic.md`。

**受入条件**
- 「明日の面接、体調不良で欠席させてください」に `欠席・遅刻` が付与される
- 辞書に該当しないメッセージは `その他` になる

### P1-6. 緊急度の自動判定
用件タグ＋経過時間から `緊急 / 通常 / 低` を算出。**欠席・遅刻は無条件で緊急**。
判定表と再計算タイミングは `business-logic.md`。

**受入条件**：欠席連絡が届いた瞬間に、その学生が一覧の最上位に移動する

### P1-7. フィルタ＆ソート
対応ステータス／選考ステータス／用件タグ／緊急度／担当者で絞り込み。
デフォルトソートは `ピン留め → 緊急度 → 経過時間（長い順）`。「自分の担当のみ」トグルを用意。

**受入条件**
- 「要返信 かつ 緊急」で絞ると該当学生のみ表示される
- 条件をクリアすると全件に戻る

### P1-8. 未対応サマリーバー
画面上部に「要返信 7件・緊急 2件・24h超 1件」を常時表示。各項目クリックでフィルタ適用。

**受入条件**：学生からメッセージが届くと、リロードせず件数が増える

---

## P2｜Should（Day2 後半）

| ID | 要件 | 工数 | 実装順 |
| --- | --- | --- | --- |
| P2-3 | 返信後の自動ステータス遷移 | S | 1 |
| P2-1 | 定型文コマンド | M | 2 |
| P2-2 | 定型文の変数自動補完 | M | 3 |
| P2-7 | 未読・既読管理 | S | 4 |
| P2-8 | ピン留め／フラグ | S | 5 |
| P2-9 | 担当者アサイン表示 | S | 6 |
| P2-4 | 学生プロフィールパネル | M | 7 |
| P2-5 | 申し送りメモ（共有メモ） | M | 8 |
| P2-6 | メモの共有昇格ボタン | S | 9 |

### P2-3. 返信後の自動ステータス遷移 ★定着の鍵
遷移表は `business-logic.md`。**手動更新を強要しないことが定着の鍵。**

**受入条件**：返信を送るだけでステータスチップの色が変わり、一覧の並び順が下がる

### P2-1. 定型文コマンド
`/` 入力でスニペット一覧。`/合格` `/不合格` `/督促` `/日程案内` `/面接前日リマインド` を初期登録。
課題 C-3「スプシからコピペで2〜3分」を数秒に短縮する。

**受入条件**：`/合` と入力すると候補が絞られ、Enter で本文が入力欄に展開される

### P2-2. 定型文の変数自動補完
`{学生名}` `{面接日時}` `{会議室}` 等を自動置換。**送信前に編集可能**（定型文の冷たさへの配慮）。

**受入条件**
- `/日程案内` を展開すると学生名と面接日時が実データで埋まる
- 面接日時が未登録の学生では `【未設定：面接日時】` の警告表示が残る

### P2-7. 未読・既読管理
一覧の未読バッジ。開いたら既読化。詳細は B-3 に準拠。

### P2-8. ピン留め／フラグ
`rooms.is_pinned` をトグルし、ソートの第1キーとする。

### P2-9. 担当者アサイン表示
一覧に担当者名を表示。**未アサインは警告色**（オレンジ枠＋「未割当」）。

### P2-4. 学生プロフィールパネル ★引き継ぎ課題への回答
チャット右側に常時表示（幅280px）。氏名／大学・学部／卒業年／選考ステータス／次回面接日時／会議室／担当面接官／担当人事／申し送りメモ／日程調整進捗。各項目インライン編集可能。

**受入条件**：担当外の人事がルームを開いても、その学生の状況が1画面で把握できる

### P2-5. 申し送りメモ（共有メモ）
既存メモを拡張し、**個人メモ／チーム共有メモ**をタブで切替。共有メモは `memo:updated` でリアルタイム配信。
課題 C-4「担当者不在でも代われる」を実現する。

**受入条件**：人事Aが共有メモを書くと、人事Bの画面にリロードなしで表示される

### P2-6. メモの共有昇格ボタン
個人メモを1クリックで `scope='shared'` に変更する。

---

## P3｜Could（Day3 午前）★1つだけ選ぶ

| ID | 要件 | 判断 |
| --- | --- | --- |
| **P3-1** | AI緊急度判定・要約（Gemini API） | **候補1** |
| **P3-4** | 日程調整ステータストラッカー | **候補2（隠れた本命）** |
| P3-2 | 対応状況ダッシュボード | 実装しない |
| P3-3 | リマインダー通知 | 実装しない |
| P3-5 | タイムライン／履歴ビュー | 実装しない |
| P3-6 | メッセージ全文検索（FTS5） | 実装しない |
| P3-7 | 一斉送信（イベント案内） | 実装しない |

### P3-1. AI 緊急度判定・要約
P1-6 のルールベースを Gemini API に置換。「今日の要対応3件」を自動要約。**デモ映え最大**。
**フォールバック要件は必須**（`business-logic.md` §7）。

**受入条件**：API を意図的に停止しても、アプリが停止せずルールベース判定で動作を継続する

### P3-4. 日程調整ステータストラッカー ★隠れた本命
`候補日提示済 → 面接官確認中 → 会議室未押さえ → 確定` の**進捗だけ**を可視化。
**カレンダー連携はしない。** 課題 C-5 に直撃しつつ、実装は「ステータスの一種を増やす」だけで済む。

**受入条件**：プロフィールパネルから3クリック以内で進捗を「確定」まで進められる

---

## スコープ外（実装しない）

モバイル対応／レスポンシブ／PWA、友だち検索・追加・ブロック、グループ作成・招待 UI、外部カレンダー連携、ファイル・画像の送受信、メール／SMS 連携

---

## 未決事項

| # | 事項 | 期限 |
| --- | --- | --- |
| Q-1 | 学生側 UI をどこまで作り込むか（現状は送受信のみの想定） | Day1 中 |
| Q-2 | ルームの初期作成方法（シード固定か、学生登録時に自動生成か） | Day1 中 |
| Q-3 | P3 は P3-1（AI）と P3-4（日程調整）のどちらを採用するか | Day2 夜 |
| Q-4 | `admin`（課長）ロールの権限差をデモで見せるか | Day2 中 |
| Q-5 | 受信箱の検索窓を全文検索にするか、氏名検索のみにするか | Day2 中 |
